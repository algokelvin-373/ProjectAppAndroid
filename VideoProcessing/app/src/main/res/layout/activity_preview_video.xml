<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:background="@color/black"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.exoplayer2.ui.StyledPlayerView
        android:id="@+id/playerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_gravity="center"
        android:background="#000000"
        android:keepScreenOn="true"
        app:layout_constraintBottom_toTopOf="@+id/timelineSectionLayout"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:use_controller="false" />

    <!-- STRIP THUMBNAIL + RANGE -->
    <LinearLayout
        android:id="@+id/timelineSectionLayout"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <!-- label waktu kiri/kanan -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="6dp">

            <TextView
                android:id="@+id/lblStart"
                android:text="00:00"
                android:textColor="#FFFFFF"
                android:textSize="12sp"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"/>

            <TextView
                android:id="@+id/lblEnd"
                android:text="00:08"
                android:textColor="#FFFFFF"
                android:textSize="12sp"
                android:gravity="end"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"/>
        </LinearLayout>

        <FrameLayout
            android:id="@+id/timelineContainer"
            android:layout_width="match_parent"
            android:layout_height="84dp">

            <!-- thumbnails -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/thumbStrip"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:overScrollMode="never"
                android:scrollbars="none" />

            <!-- overlay box white -->
<!--            <com.sbimv.mvicalls.ui.SelectionOverlayView-->
<!--                android:id="@+id/selectionOverlay"-->
<!--                android:layout_width="match_parent"-->
<!--                android:layout_height="match_parent"-->
<!--                android:clickable="false"/>-->

            <!-- slider transparent (just for interaction) -->
            <com.google.android.material.slider.RangeSlider
                android:id="@+id/rangeSlider"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingLeft="8dp"
                android:paddingRight="8dp"
                android:visibility="gone"
                app:thumbRadius="10dp"
                app:haloRadius="0dp"
                app:tickVisible="false"
                app:trackHeight="0dp"
                app:trackColorInactive="@android:color/transparent"
                app:trackColorActive="@android:color/transparent"
                app:thumbTint="@android:color/white"
                app:haloColor="@android:color/white" />
        </FrameLayout>
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>